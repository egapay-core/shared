package utils

type YesNoEnum string

const (
	Yes YesNoEnum = "YES"
	No  YesNoEnum = "NO"
)

type ActiveClosedEnum string

const (
	Active ActiveClosedEnum = "ACTIVE"
	Closed ActiveClosedEnum = "CLOSED"
)
